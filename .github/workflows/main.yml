name: TestDeploy25
on: 
  push: 
    branches: [ main, qa, develop]
env: 
  env_var: ${{ secrets.URL }}
jobs:
  Deploy-environment-test:
    runs-on: ubuntu-latest
    steps:
      - name: "Say Hello Env variables"
        run: |
         echo "Value of variables is ${{ vars.URL }} ..."
         echo "      of variables is ${{ inputs.environment }} ..."
         echo "second of variables is $env_var ..."
